# IMPORTANT!
# this config expects the following things to be available:
# - dunst
# - picom
# - dex
# - xss-lock
# - i3lock
# - nm-applet
# - wezterm
# - feh (for setting wallpaper)
# - maim + slop + xclip (for taking screenshots)
# - fantasque sans mono
# - symbols nerd font

# no launcher is provided here.
# i install ulauncher and launch it with dex.

# logo key
set $mod Mod4

# home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# your preferred terminal emulator
set $term wezterm

# your preferred screenshot utility
set $screenshot maim -s | xclip -selection clipboard -target image/png

# workspace names
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

exec --no-startup-id dunst
exec --no-startup-id picom
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

include conf.d/*.conf

